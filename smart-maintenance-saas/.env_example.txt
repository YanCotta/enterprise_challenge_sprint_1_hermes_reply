# Smart Maintenance SaaS - CLOUD PIVOT CONFIGURATION

# Core
ENV=production
LOG_LEVEL=INFO

# API / Security (We will use stronger keys for production)
API_KEY=
SECRET_KEY=
JWT_SECRET=

# --- DATABASE (CLOUD) ---
# This is your new Timescale Cloud DB, configured for the FastAPI App (asyncpg)
DATABASE_URL=

# --- REDIS (CLOUD) ---
# This is your new Render Redis instance.
REDIS_URL=

# --- MLFLOW (CLOUD PIVOT) ---

# 1. This tells your notebooks/scripts to talk to the MLflow server
#    running in the Docker container (use service name for Docker networking).
MLFLOW_TRACKING_URI=

# 2. This tells that local MLflow server to use your CLOUD DB
#    as its backend. Note the "postgresql://" (NO asyncpg) for MLflow.
MLFLOW_BACKEND_STORE_URI=
# 3. This tells MLflow to save model files (artifacts) to your CLOUD S3 bucket.
MLFLOW_ARTIFACT_ROOT=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=

# --- UI (We will fill this in after deploying the API) ---
API_BASE_URL=

# --- NOTIFICATIONS (Disabled for cloud setup) ---
EMAIL_SMTP_HOST=smtp.example.com
EMAIL_SMTP_PORT=587
EMAIL_SMTP_TLS=true
EMAIL_SMTP_USER=disabled
EMAIL_SMTP_PASS=disabled
EMAIL_TO=disabled@example.com