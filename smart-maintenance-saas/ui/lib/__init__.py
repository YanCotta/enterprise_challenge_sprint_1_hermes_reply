"""UI library package initialization.

Exposes shared client utilities (see api_client). Additional shared UI helpers
can be added here in the future (e.g., caching wrappers, formatting functions).
"""

__all__ = ["api_client"]
